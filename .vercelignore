# Vercel deploys the static frontend only; server + data belong on Fly/R2.
server.js
Dockerfile
fly.toml
.dockerignore
# Only ignore the *root* data/ (grib/gfs/ecmwf/reference) — not public/data/
# which holds shipped static JSON (named-spots, coast-points).
/data/
node_modules/
.github/
